<Page
    x:Class="TvBand.Uwp.Views.ShellView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:cm="using:Caliburn.Micro"
    xmlns:local="using:TvBand.Uwp.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <SplitView IsPaneOpen="{Binding PaneOpen}" DisplayMode="CompactOverlay" CompactPaneLength="50" OpenPaneLength="150">
        <SplitView.Content>
            <Frame cm:Message.Attach="[Event Loaded] = [SetupNavigationService($source)]" DataContext="{x:Null}" />
        </SplitView.Content>
        <SplitView.Pane>
            <StackPanel>
                <ToggleButton IsChecked="{Binding PaneOpen, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" FontFamily="Segoe MDL2 Assets" Content="&#xE700;" Width="50" Height="50" Background="Transparent"/>
                <Button Command="{Binding NavigateToSettingsCommand}" Content="&#xE115;" FontFamily="Segoe MDL2 Assets" Width="50" Height="50" Background="Transparent" />
            </StackPanel>
        </SplitView.Pane>
    </SplitView>
</Page>
